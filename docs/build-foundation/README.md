# Build foundation

Canonical method lives in omcoda-hq (`register-manual/` + `omcoda-register` skill).
This product owns its own `docs/register/*` and `docs/sme/*` — do not substitute the public HQ site for product docs.
